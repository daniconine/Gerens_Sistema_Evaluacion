#consultas query

#seleccionar la informacion de un tabla
use ana_rosa;
select * from Programa;

#borrar datos de una tabla 
 use ana_rosa;
DELETE FROM Docente
WHERE id_docente = 90043

#modificar el ancho de datos de una columna
ALTER TABLE Programa
MODIFY Prog_Nombre
varchar(40);

#Inseertar datos en Tabla PRograma
use ana_rosa;
INSERT INTO Programa (id_programa, Prog_Nombre) VALUES 
 ('01','Maestría en Gestión Minera'); 
 
 use ana_rosa;
INSERT INTO Docente(id_docente, Doc_Primer_Nombre )
	VAlUES("90043",)
 
 use ana_rosa;
DELETE FROM Docente
WHERE id_docente = 90043

ALTER TABLE Curso
MODIFY Cur_Nombre
varchar(100);

use ana_rosa;
INSERT INTO Programa (id_programa, Prog_Nombre) VALUES 
 ('01','Maestría en Gestión Minera'); 
 
use ana_rosa;
ALTER TABLE Estudiante ADD Est_Cod_Matricula int(10) 
AFTER id_estudiante;

  
 #consultas query
use ana_rosa;
select * from Docente;

use ana_rosa;
INSERT INTO Docente(


#consultas query
SET SQL_SAFE_UPDATES = 0;
update Curso
Set Cur_Nombre= "Laboratorio de Comunicación y Coaching II" where Cur_Nombre = "Desarrollo de habilidades gerenciales I" ;
SET SQL_SAFE_UPDATES = 1;

use ana_rosa;
select Cur_Nombre, Cur_Pensum, Cur_Ciclo, id_curso
from Curso Where Cur_Pensum = "2022" and Cur_Ciclo="C2";

ANd Cur_Ciclo="C4";

select*from Moodle;

select*
from Curso Where Cur_Pensum = "2022" and Cur_Ciclo="C2";

